# NPM Configuration for JJB Gallery
# Configured to use external USB drive for storage-intensive operations

# Auto-install peer dependencies
auto-install-peers=true

# Save exact versions (not ranges) for better reproducibility
save-exact=false

# Use npm registry
registry=https://registry.npmjs.org/

# Legacy peer deps (for compatibility with some packages)
legacy-peer-deps=false

# Fund messages
fund=false

# Audit level
audit-level=moderate

# Package lock
package-lock=true

# Engine strict mode
engine-strict=false

# ============================================
# EXTERNAL DRIVE CONFIGURATION
# ============================================
# Store npm cache on external USB drive
cache=/Volumes/SEALED/DSHB/GALLERY/.npm-cache

# Store global packages on external drive (optional)
# prefix=/Volumes/SEALED/DSHB/GALLERY/.npm-global

# Temporary files location
tmp=/Volumes/SEALED/DSHB/GALLERY/.npm-tmp
